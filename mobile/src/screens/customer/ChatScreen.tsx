import React from 'react';
import { ChatWorkspace } from '../../components/ChatWorkspace';

const ChatScreen = () => (
  <ChatWorkspace
    title="Support Chat"
    subtitle="Reach the Loka operations desk instantly for approvals, delays, or delivery issues."
  />
);

export default ChatScreen;
